{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": ["run"]
            },
            "cwd": "${workspaceFolder}/user",
            "sourceLanguages": ["rust"]
        }
    ]
}
